
const initUsers = {
    users: [],
    severConnect: false,
    errors: null
};

const users = (state = initUsers, action) => {
    switch (action.type) {
        //TODO add actions
        default:
            return state;
    }
}

export default users;